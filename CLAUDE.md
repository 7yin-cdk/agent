# Claude.md —— 数据库运维 Agent 项目说明与代码规范

## 一、项目背景

本项目旨在构建一个**智能数据库运维 Agent**，该 Agent 能够：

- 通过 **RAG（Retrieval-Augmented Generation）** 技术，从已有的运维文档、故障案例、最佳实践等知识库中检索相关信息，辅助运维决策。
- 通过调用**外部工具**（如 SQL 执行器、监控 API、备份恢复脚本、集群管理命令等），直接执行数据库运维操作，包括但不限于：
    - 性能诊断与调优
    - 故障排查与恢复
    - 容量规划与扩缩容
    - 数据备份与迁移
    - 集群状态巡检

Agent 的核心能力是结合知识检索与自动化执行，降低人工运维成本，提升响应速度和准确性。

---

## 二、代码规范

为确保代码的可读性、可维护性和团队协作的一致性，所有代码必须遵循以下规范：

### 2.1 简洁与去冗余

- **避免重复代码**：相同或相似的逻辑必须抽取为公共函数、类或模块。
- **保持方法单一职责**：每个方法只做一件事，且功能明确。
- **善用语言特性**：如 Java 的 Stream API、Optional、Lambda 等，以精简代码。
- **删除无用代码**：废弃的变量、导入、注释等必须及时清理。
- **控制代码行数**：单个方法不超过 50 行（特殊情况除外），单个类不超过 500 行。

### 2.2 注释规范

- **所有注释必须使用 Java 风格的多行注释**，即 `/* ... */`，即使在其他语言（如 Python、Shell、Go）中也不例外。
    - 例如在 Python 中，不要使用 `#` 单行注释或 `'''` 文档字符串，而应使用 `/* ... */` 包裹注释内容。
    - 在 Shell 脚本中，同样使用 `/* ... */`。
- **注释必须详细且有意义**，包括但不限于：
    - 文件头部：说明文件用途、作者、创建日期。
    - 方法：说明功能、参数、返回值、异常情况。
    - 复杂逻辑：解释算法思路、为什么这样实现、注意事项。
    - 关键变量：解释其含义和取值范围。
- **注释应使用英文或中文**（团队统一），推荐使用中文便于沟通，但专业术语可保留英文。

