# Coding Agent 开发规范

## 1. 代码注释规范

### 1.1 注释要求

所有涉及业务逻辑的代码都必须添加注释。

这里的“业务逻辑”包括但不限于：

* 业务流程和处理流程
* 条件判断及其业务原因
* 数据转换、数据过滤
* 状态变更
* 异常处理
* 重试、超时、降级等机制
* Agent 的执行流程、工具调用流程
* RAG 检索、重排序、上下文处理等业务流程
* 其他无法仅通过代码直观看出业务含义的逻辑

不需要为每一行代码添加注释。

对于显而易见的代码，不要添加没有实际意义的注释。

错误示例：

```java
// 创建用户
User user = new User();
```

```java
// 判断用户是否为空
if (user == null) {
    return;
}
```

正确示例：

```java
// 过滤已经失效的会话，避免失效会话继续参与 Agent 上下文构建
sessions.removeIf(Session::isExpired);
```

```java
// 限制 Agent 最大执行步数，防止模型持续调用工具导致执行循环无法结束
if (step >= MAX_REACT_STEPS) {
    return buildMaxStepResponse();
}
```

### 1.2 注释风格

所有代码注释必须保持统一风格，并使用浅显、直接、容易理解的中文。

注释应该重点说明：

1. 这段业务逻辑为什么存在
2. 这段逻辑解决什么问题
3. 这段逻辑有什么特殊行为或限制

优先使用简洁的中文描述，避免使用复杂、晦涩的技术表达。

推荐：

```java
// 工具调用失败后最多重试 3 次，避免单次网络异常直接导致 Agent 执行失败
```

不推荐：

```java
// 基于指数退避策略对工具调用进行容错处理，以增强系统在瞬时故障场景下的鲁棒性
```

如果代码本身已经能够清楚表达含义，则不需要重复解释代码。

### 1.3 注释位置

注释应尽量放在对应业务逻辑的前面或附近，使阅读代码时能够直接理解对应逻辑。

对于较长的业务流程，可以在关键步骤前添加注释：

```java
// 第一步：根据用户问题判断当前请求的类型
IntentType intent = intentService.classify(query);

// 第二步：知识类问题进入 RAG 检索流程
if (intent == IntentType.KNOWLEDGE) {
    return ragService.search(query);
}

// 第三步：操作类问题进入 Agent 工具调用流程
return agentService.execute(query);
```

不要为了满足注释要求而给大量简单代码添加重复注释。

---

## 2. 需求边界规范

### 2.1 只实现用户明确提出的需求

编码时只完成用户明确提出的需求。

不要擅自增加用户没有要求的功能、重构、优化或行为变化。

例如用户要求：

> 给用户查询接口增加分页。

只能实现分页相关修改，不要同时：

* 修改数据库表结构
* 重构 Service 层
* 更换 ORM
* 增加缓存
* 修改其他接口
* 修改无关代码
* 增加用户没有要求的功能

### 2.2 发现其他问题时

如果在实现需求的过程中发现与当前需求无关的问题：

* 可以向用户提出建议
* 不要擅自修改

例如：

> 我发现这个方法存在缓存击穿风险，但这个问题与当前需求无关。建议后续增加缓存保护，但本次暂不修改。

只有在该问题会直接导致当前需求无法正确实现，或者当前代码存在明确错误时，才可以修改。

### 2.3 错误可以修复

如果发现当前代码存在明确错误，并且该错误会影响当前需求的实现、运行或正确性，可以直接修复。

例如：

* 编译错误
* 明确的运行时错误
* 明确的空指针问题
* 明确的参数错误
* 明确的逻辑错误
* 会导致当前需求无法正常工作的错误

修复错误时，应尽量控制修改范围，不要借机进行无关重构。

---

## 3. 多方案处理规范

### 3.1 存在多个合理实现方案时，必须先询问

如果用户提出的需求存在多个合理的实现方案，并且不同方案会影响：

* 系统架构
* 技术选型
* 数据结构
* 接口设计
* 性能
* 可维护性
* 业务行为

则不要直接选择其中一种方案进行实现。

应该先向用户说明主要方案及其核心区别，然后询问用户选择哪一种。

例如：

> 这个需求有两种实现方式：
>
> 方案 A：使用 Redis 保存状态，优点是实现简单、读取速度快。
>
> 方案 B：使用 MySQL 保存状态，优点是数据持久化更直接。
>
> 两种方案都会影响后续的数据设计，请确认采用哪一种方案后我再修改代码。

### 3.2 不需要询问的情况

如果不同实现方式只是代码层面的等价写法，并不会影响系统设计、行为或后续维护，则可以直接选择合理的实现方式。

例如：

```java
if (user == null) {
    return;
}
```

和：

```java
if (Objects.isNull(user)) {
    return;
}
```

这种情况不需要询问用户。

### 3.3 用户已经指定方案时

如果用户已经明确指定了技术方案、实现方式或技术组件，则严格按照用户指定的方案实现。

不要在没有必要的情况下更换技术方案。

---

## 4. 修改代码的基本原则

每次修改代码时遵循以下原则：

1. 只修改完成当前需求所需要的代码。
2. 尽量减少无关文件和无关代码的修改。
3. 不因为个人偏好擅自重构代码。
4. 不擅自增加功能。
5. 不擅自更换技术方案。
6. 不擅自修改已有业务行为。
7. 发现与当前需求无关的问题，可以提出建议，但不要修改。
8. 发现会影响当前需求的明确错误，可以修复。
9. 业务逻辑必须添加清晰、统一、易懂的中文注释。
10. 如果需求存在会影响系统设计或行为的多个合理方案，先询问用户，再开始编码。

## 5. 执行顺序

处理用户的编码需求时，按照以下顺序执行：

1. 理解用户明确提出的需求。
2. 检查当前代码，确定实现需求所需的最小修改范围。
3. 判断是否存在多个会影响系统设计或行为的合理实现方案。
4. 如果存在多个方案，先向用户说明方案差异并询问选择。
5. 如果不存在需要确认的方案，直接实现需求。
6. 编写或修改代码时，为涉及业务逻辑的部分添加统一、浅显易懂的中文注释。
7. 检查修改是否引入编译错误、运行错误或明显逻辑错误。
8. 不主动修改与当前需求无关的代码。
9. 最终说明本次实际修改了什么，以及发现但没有修改的其他问题（如果有）。
