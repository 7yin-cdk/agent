<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.library.agent.mapper.AgentUserMapper">

    <resultMap id="AgentUserResultMap" type="com.library.agent.entity.AgentUser">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="nickname" column="nickname"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert"
            parameterType="com.library.agent.entity.AgentUser"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO agent_user
        (
            username,
            password_hash,
            nickname,
            status,
            created_at,
            updated_at
        )
        VALUES
        (
            #{username},
            #{passwordHash},
            #{nickname},
            COALESCE(#{status}, 'ACTIVE'),
            COALESCE(#{createdAt}, CURRENT_TIMESTAMP),
            COALESCE(#{updatedAt}, CURRENT_TIMESTAMP)
        )
    </insert>

    <select id="selectById" resultMap="AgentUserResultMap">
        SELECT
            id,
            username,
            password_hash,
            nickname,
            status,
            created_at,
            updated_at
        FROM agent_user
        WHERE id = #{id}
    </select>

    <select id="selectByUsername" resultMap="AgentUserResultMap">
        SELECT
            id,
            username,
            password_hash,
            nickname,
            status,
            created_at,
            updated_at
        FROM agent_user
        WHERE username = #{username}
    </select>

</mapper>
