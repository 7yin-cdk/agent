spring:
  application:
    name: Agent
  datasource:
    url: jdbc:mysql://localhost:3306/agent?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: zq2892294059
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 100MB

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.library.agent.entity
  configuration:
    map-underscore-to-camel-case: true

minio:
  endpoint: http://localhost:9000
  access-key: admin
  secret-key: zq2892294059
  bucket: rag-bucket

